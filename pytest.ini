[pytest]
minversion = 8.0
testpaths = tests
addopts = -ra --strict-markers
markers =
    slow: slow tests
    benchmark: benchmark tests
    integration: integration tests
    gpu: GPU-accelerated tests (require CUDA)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
